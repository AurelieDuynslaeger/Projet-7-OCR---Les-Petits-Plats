export function searchTag(selectedTag) {
    return `
        <div class="tag bg-customYellow font-manrope rounded-xl flex">
            <p>x</p>
            <p>${selectedTag}</p>
        </div>
    `;
}